import 'package:flutter/material.dart';

class BookmarkItem extends StatelessWidget {
const BookmarkItem({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}